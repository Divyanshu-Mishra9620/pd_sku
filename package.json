{
  "name": "pd-sku-api",
  "version": "1.0.0",
  "description": "Product SKU variant generator using Gemini Batch API",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "node --check src/index.js && node --check src/rabbitmq.js && node --check src/gemini.js && node --check src/cloudinary.js && node --check src/prompt.js"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "amqplib": "^2.0.1",
    "cloudinary": "^2.0.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
